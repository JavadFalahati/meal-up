package com.jwdfhi.meal_up.utils

import androidx.compose.ui.graphics.Color

object ManagementSettings {
    var PrimaryColor: Color = Color(0xD3FF7C1C)
    var Primary80Color = PrimaryColor.copy(0.8f)
    var Primary60Color = PrimaryColor.copy(0.6f)
    var LightPrimaryColor = PrimaryColor.copy(0.3f)
}